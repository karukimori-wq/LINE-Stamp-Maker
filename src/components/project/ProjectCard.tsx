'use client';
import Link from 'next/link';
import type { Project } from '@/types/project';

export function ProjectCard({project,onDuplicate,onDelete}:{project:Project;onDuplicate:(id:string)=>void;onDelete:(id:string)=>void}){const type=project.stampType==='animated'?'アニメーション':'通常';return <article className="rounded-3xl border border-zinc-200 bg-white p-4 shadow-sm"><div className="flex gap-3"><div className="grid h-20 w-20 shrink-0 place-items-center rounded-2xl bg-emerald-50 text-4xl">{project.stampType==='animated'?'🎞️':'🐱'}</div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h3 className="truncate font-bold">{project.name}</h3><span className="shrink-0 rounded-full bg-zinc-100 px-2 py-1 text-[10px] font-bold text-zinc-600">{type}</span></div><p className="mt-1 text-sm text-zinc-600">{project.character.name || 'キャラクター未設定'}</p><p className="mt-1 text-xs text-zinc-500">現在: STEP {project.currentStep}</p><p className="text-xs text-zinc-400">更新: {new Date(project.updatedAt).toLocaleString('ja-JP')}</p></div></div><div className="mt-4 grid grid-cols-3 gap-2 text-sm"><Link href={`/projects/${project.id}`} className="rounded-xl bg-emerald-500 px-3 py-2 text-center font-semibold text-white">続きから</Link><button onClick={()=>onDuplicate(project.id)} className="rounded-xl border px-3 py-2">複製</button><button onClick={()=>onDelete(project.id)} className="rounded-xl border border-red-200 px-3 py-2 text-red-600">削除</button></div></article>}
