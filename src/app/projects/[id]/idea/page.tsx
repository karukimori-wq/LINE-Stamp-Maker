'use client';
import { useEffect, useState } from 'react';
import { useParams, useRouter } from 'next/navigation';
import { getProject } from '@/lib/db';
import type { Project } from '@/types/project';
export default function IdeaPlaceholder(){const{id}=useParams<{id:string}>();const router=useRouter();const[project,setProject]=useState<Project|null>(null);const[error,setError]=useState('');useEffect(()=>{getProject(id).then(p=>p?setProject(p):setError('プロジェクトが見つかりません。')).catch(()=>setError('プロジェクトを読み込めませんでした。'));},[id]);return <main className="mx-auto min-h-dvh max-w-md bg-zinc-50 px-5 pb-10 pt-[calc(env(safe-area-inset-top)+1rem)]"><button onClick={()=>router.push('/')} className="min-h-11 text-2xl">‹</button><div className="mt-3 flex items-center justify-between"><div><p className="text-xs font-semibold text-emerald-600">STEP 1 / 5</p><h1 className="mt-1 text-xl font-bold">アイデア</h1></div><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs text-emerald-700">Phase 3で実装</span></div>{error?<p className="mt-8 rounded-2xl bg-red-50 p-3 text-sm text-red-700">{error}</p>:<section className="mt-8 rounded-3xl border bg-white p-5 shadow-sm"><p className="text-sm text-zinc-500">プロジェクト</p><h2 className="mt-1 text-lg font-bold">{project?.name??'読み込み中...'}</h2><p className="mt-6 text-sm leading-6 text-zinc-600">Phase 1・2ではプロジェクト基盤、PWA、IndexedDB保存、ホーム、新規作成、複製、削除まで実装しています。STEP 1の入力フォームとプロンプト生成はPhase 3で追加します。</p></section>}</main>}
