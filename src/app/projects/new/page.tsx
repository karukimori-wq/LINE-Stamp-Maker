'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { createProject } from '@/lib/db';
import { PrimaryButton } from '@/components/ui/PrimaryButton';
export default function NewProjectPage(){const[name,setName]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();if(!name.trim()){setError('プロジェクト名を入力してください。');return;}try{setBusy(true);const project=await createProject(name);router.push(`/projects/${project.id}/idea`);}catch{setError('プロジェクトを保存できませんでした。もう一度お試しください。');setBusy(false);}}return <main className="mx-auto min-h-dvh max-w-md bg-white px-5 pb-[calc(env(safe-area-inset-bottom)+2rem)] pt-[calc(env(safe-area-inset-top)+1rem)]"><button onClick={()=>router.back()} className="min-h-11 text-2xl">‹</button><div className="mt-16"><h1 className="text-center text-2xl font-bold">新しいプロジェクト</h1><form onSubmit={submit} className="mt-10"><label className="text-sm font-semibold">プロジェクト名</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="例：ゆるねこ日常スタンプ" className="mt-2 min-h-14 w-full rounded-2xl border border-zinc-200 px-4 outline-none focus:border-emerald-500" autoFocus/>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}<div className="mt-20"><PrimaryButton disabled={busy}>{busy?'作成中...':'プロジェクトを作成'}</PrimaryButton></div></form></div></main>}
